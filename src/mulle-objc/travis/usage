Adds Travis-CI support, configured for mulle-objc based projects
Your project will be built with mulle-sde and tested with mulle-test
(if a test folder is present). This variant adds the mulle-clang repository
to travis.

