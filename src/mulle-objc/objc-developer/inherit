# always runtime before buildtool to pick up language
mulle-objc/objc;runtime
mulle-objc/objc-cmake;buildtool

#
# Not using the \"inherit\" file" here, but specify
# the dependencies like this, so we "clobber" what we
# inherit as demofiles without overwriting existing
# user files.
#
# Because of the quirky way mulle-sde does this, the
# order is reversed.
#
mulle-objc/objc-demo;extra
